# Machine Learning & Prediction: RandomForestRegressor is trained with
# unbounded max_depth, so the pickled model is ~170 MB - over GitHub's
# 100 MB blob limit. Fully reproducible (fixed random_state) by running
# `python scripts/train_regression.py` from that module's directory.
Machine Learning & Prediction/Models/heat_risk_model.pkl

.vercel
.env*

# Python bytecode
__pycache__/
*.pyc

# pytest
.pytest_cache/

# Pure pipeline intermediate: 3.3 MB, regenerated by scripts/preprocess.py
# in seconds and read by nothing but the next two scripts.
Machine Learning & Prediction/Results/preprocessed.csv

# The deck is a large binary and is maintained outside the repository.
*.pptx
