# Machine Learning & Prediction — pinned dependencies
# Versions are those verified working for this module (Python 3.13).
pandas==3.0.5
numpy==2.4.6
scipy==1.18.0
scikit-learn==1.9.0
shapely==2.1.2
matplotlib==3.11.0
joblib==1.5.3

# Deliberately NOT required: geopandas / fiona / pyproj.
# The .geo column is plain GeoJSON, so shapely + json cover all geometry work
# without pulling in the GDAL stack. See README section 8.
